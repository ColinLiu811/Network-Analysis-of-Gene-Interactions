# Example Data Notes

Place small, shareable sample datasets here for tutorials and notebooks.

Recommended files:
- `sample_interactions.csv` (cleaned interaction-style input)
- `sample_hub_genes.csv` (small hub ranking output)
- `sample_network.graphml` (small graph for quick demos)

Keep files lightweight so examples run quickly in local development and CI.
