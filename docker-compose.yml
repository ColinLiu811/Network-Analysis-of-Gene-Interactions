version: "3.9"

services:
  network-analysis:
    build:
      context: .
      dockerfile: Dockerfile
    image: network-analysis-gene-interactions:latest
    container_name: network-analysis
    working_dir: /app
    volumes:
      - ./:/app
    command: ["python", "run_pipeline.py", "--help"]
